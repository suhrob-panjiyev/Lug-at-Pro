services:
  - type: web
    name: lugatpro-bot
    env: python
    plan: free
    buildCommand: pip install -r bot/requirements.txt
    startCommand: uvicorn bot.webhook_app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.3